%p
  Welcome to Presentations
  
- form_for :presentation, :url => {:action => :create}, :html => { :multipart => true } do |p|
  = p.label :title
  = p.text_field :title
  = p.label :description
  = p.text_field :description
  = p.label :slide_deck
  = p.file_field :slide_deck
  = p.label :event
  = select_tag(:event, options_from_collection_for_select(@events, :id, :formatted_date))
  = p.submit
   